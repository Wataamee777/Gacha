<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title>🎛 ガチャ管理 | <%= guildId %></title>
  <link rel="stylesheet" href="/static/style.css">
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      margin: 0;
      background: #f7f7fb;
      color: #222;
    }

    header {
      background: #5865f2;
      color: #fff;
      padding: 1rem 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    h1 {
      margin: 0;
      font-size: 1.5rem;
    }

    main {
      max-width: 900px;
      margin: 2rem auto;
      background: #fff;
      padding: 2rem;
      border-radius: 10px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }

    h2 {
      border-left: 4px solid #5865f2;
      padding-left: 0.5rem;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 0.5rem;
      margin-bottom: 2rem;
    }

    input, select {
      padding: 0.6rem;
      border: 1px solid #ccc;
      border-radius: 6px;
      font-size: 1rem;
    }

    button {
      background: #5865f2;
      color: white;
      border: none;
      padding: 0.6rem 1rem;
      border-radius: 6px;
      cursor: pointer;
      font-size: 1rem;
      transition: 0.2s;
    }

    button:hover {
      background: #4752c4;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 1.5rem;
    }

    th, td {
      padding: 0.7rem;
      text-align: left;
      border-bottom: 1px solid #ddd;
    }

    tr:hover {
      background: #f4f4ff;
    }

    .delete-btn {
      background: #f04747;
      color: white;
      border: none;
      padding: 0.4rem 0.8rem;
      border-radius: 6px;
      cursor: pointer;
      transition: 0.2s;
    }

    .delete-btn:hover {
      background: #c03434;
    }

    .edit-btn {
      background: #43b581;
      color: white;
      border: none;
      padding: 0.4rem 0.8rem;
      border-radius: 6px;
      cursor: pointer;
      transition: 0.2s;
      margin-right: 4px;
    }

    .edit-btn:hover {
      background: #3aa374;
    }

    @media (max-width: 600px) {
      table, thead, tbody, th, td, tr {
        display: block;
      }
      tr {
        margin-bottom: 1rem;
        border: 1px solid #ddd;
        border-radius: 8px;
        padding: 0.5rem;
      }
      th {
        display: none;
      }
      td {
        display: flex;
        justify-content: space-between;
      }
      td::before {
        content: attr(data-label);
        font-weight: bold;
        color: #555;
      }
    }
  </style>
</head>

<body>
  <header>
    <h1>🎛 ガチャ管理 - Guild: <%= guildId %></h1>
    <a href="/dashboard" style="color: white; text-decoration: underline;">← サーバー一覧に戻る</a>
  </header>

  <main>
    <h2>🎰 新しいガチャを作成</h2>
    <form action="/gacha/<%= guildId %>/create" method="POST">
      <input type="text" name="name" placeholder="ガチャ名（例: 桜ガチャ）" required>
      <input type="text" name="plex" placeholder="呼び出しキーワード（例: 桜）">
      <input type="text" name="channel" placeholder="チャンネルID（任意）">
      <input type="text" name="role" placeholder="ロールID（任意）">
      <input type="number" name="delete_after_days" placeholder="削除までの日数（任意）">
      <button type="submit">作成</button>
    </form>

    <h2>📜 登録済みガチャ一覧</h2>

    <% if (gachas.length === 0) { %>
      <p>まだガチャは登録されていません。</p>
    <% } else { %>
      <table>
        <thead>
          <tr>
            <th>ガチャ名</th>
            <th>Plex</th>
            <th>チャンネル</th>
            <th>ロール</th>
            <th>削除日数</th>
            <th>操作</th>
          </tr>
        </thead>
        <tbody>
          <% gachas.forEach(g => { %>
            <tr>
              <td data-label="ガチャ名"><%= g.name %></td>
              <td data-label="Plex"><%= g.plex || '-' %></td>
              <td data-label="チャンネル"><%= g.channel_id || '-' %></td>
              <td data-label="ロール"><%= g.role_id || '-' %></td>
              <td data-label="削除日数"><%= g.delete_after_days || '-' %></td>
              <td data-label="操作">
                <button class="edit-btn" onclick="openEditForm('<%= g.name %>', '<%= g.plex || '' %>', '<%= g.channel_id || '' %>', '<%= g.role_id || '' %>', '<%= g.delete_after_days || '' %>')">編集</button>
                <form action="/gacha/<%= guildId %>/<%= g.name %>/delete" method="POST" onsubmit="return confirm('本当に削除しますか？');" style="display:inline;">
                  <button type="submit" class="delete-btn">削除</button>
                </form>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    <% } %>

    <!-- 編集フォーム（モーダル風） -->
    <div id="editModal" style="display:none; position:fixed; top:0; left:0; width:100%; height:100%; background:rgba(0,0,0,0.4); align-items:center; justify-content:center;">
      <div style="background:#fff; padding:1.5rem; border-radius:8px; width:90%; max-width:500px;">
        <h2>✏️ ガチャ編集</h2>
        <form id="editForm" method="POST">
          <input type="text" id="editName" name="name" placeholder="ガチャ名" readonly>
          <input type="text" id="editPlex" name="plex" placeholder="呼び出しキーワード">
          <input type="text" id="editChannel" name="channel" placeholder="チャンネルID">
          <input type="text" id="editRole" name="role" placeholder="ロールID">
          <input type="number" id="editDeleteDays" name="delete_after_days" placeholder="削除までの日数">
          <div style="display:flex; justify-content:flex-end; gap:0.5rem;">
            <button type="button" onclick="closeEditForm()" style="background:#ccc; color:#222;">キャンセル</button>
            <button type="submit">更新</button>
          </div>
        </form>
      </div>
    </div>
  </main>

  <script>
    function openEditForm(name, plex, channel, role, deleteDays) {
      document.getElementById('editModal').style.display = 'flex';
      document.getElementById('editName').value = name;
      document.getElementById('editPlex').value = plex;
      document.getElementById('editChannel').value = channel;
      document.getElementById('editRole').value = role;
      document.getElementById('editDeleteDays').value = deleteDays;

      document.getElementById('editForm').action = `/gacha/<%= guildId %>/${name}/edit`;
    }

    function closeEditForm() {
      document.getElementById('editModal').style.display = 'none';
    }

    window.onclick = function(event) {
      if (event.target === document.getElementById('editModal')) {
        closeEditForm();
      }
    }
  </script>
</body>
</html>
